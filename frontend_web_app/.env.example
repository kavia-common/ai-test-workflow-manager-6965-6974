# Frontend environment configuration for AI Test Workflow Manager

# Backend API base URL. If not set, the app will default to http://<host>:3001 in dev.
REACT_APP_API_BASE=http://localhost:3001

# Health check endpoint relative path on the backend
REACT_APP_HEALTHCHECK_PATH=/health

# Optional: log level for client (info, debug, warn, error)
REACT_APP_LOG_LEVEL=info

# Optional: comma-separated feature flags for experimental features
# e.g., ai-suggest,auth-placeholder
REACT_APP_FEATURE_FLAGS=ai-suggest,auth-placeholder

# Alternative variable supported for compatibility; if set, used when REACT_APP_API_BASE is not
# REACT_APP_BACKEND_URL=
